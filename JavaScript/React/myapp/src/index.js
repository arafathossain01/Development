import React from "react"; // React --> class
import ReactDOM from "react-dom/client"; // ReactDOM --> object access the html div
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
    <App />
);
