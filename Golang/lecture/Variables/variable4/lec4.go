package main
import ("fmt")

func main(){
	var (
		num int = 30;
		str string = "Hello"; 
	)

	/*
	Variable declare in a block, it increase the readability.
	*/
	
	fmt.Println(num)
	fmt.Println(str)
}